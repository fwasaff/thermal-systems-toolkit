numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.4.0
pandas>=1.3.0

# Optional: temperature-dependent fluid properties (fluid_properties.py,
# pipe_design_summary(..., temperature_celsius=...)). Everything else in
# the toolkit works without it, using the fixed WATER_RHO/WATER_MU
# constants (valid near 20 degC).
CoolProp>=6.4.0
